@using CoffeeTunes.Contracts
@using CoffeeTunes.Contracts.Franchise
@using RestEase

<div class="modal-backdrop @(IsVisible ? "show" : "")" @onclick="OnBackdropClick">
    <div class="modal-content" @onclick:stopPropagation="true">
        <div class="modal-header">
            <h2 class="modal-title">☕ Join Existing Franchise</h2>
            <button class="modal-close" @onclick="Close">✕</button>
        </div>
        
        @if (errorMessage != null)
        {
            <div class="modal-error">
                <div class="error-icon">⚠️</div>
                <p>@errorMessage</p>
            </div>
        }

        <div class="modal-body">
            <form @onsubmit="HandleSubmit">
                <div class="form-group">
                    <label for="franchise-name">Franchise Name</label>
                    <input 
                        id="franchise-name"
                        type="text" 
                        class="form-input" 
                        @bind="franchiseCode" 
                        placeholder="Enter the franchise code to join..."
                        maxlength="100"
                        disabled="@isSubmitting"
                        required />
                </div>
                
                <div class="modal-actions">
                    <button type="button" class="btn-cancel" @onclick="Close" disabled="@isSubmitting">
                        Cancel
                    </button>
                    <button type="submit" class="btn-join" disabled="@(isSubmitting || string.IsNullOrWhiteSpace(franchiseCode))">
                        @if (isSubmitting)
                        {
                            <span class="spinner">☕</span>
                            <span>Joining...</span>
                        }
                        else
                        {
                            <span>Join Franchise</span>
                        }
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@code {
    [Parameter]
    public bool IsVisible { get; set; }

    [Parameter]
    public EventCallback OnClose { get; set; }

    [Parameter]
    public EventCallback<string> OnFranchiseJoined { get; set; }

    [Inject]
    public ICoffeeTunesApi Api { get; set; } = default!;

    private string franchiseCode = string.Empty;
    private bool isSubmitting = false;
    private string? errorMessage;

    private async Task HandleSubmit()
    {
        if (string.IsNullOrWhiteSpace(franchiseCode))
        {
            return;
        }

        isSubmitting = true;
        errorMessage = null;

        try
        {
            var contract = new JoinFranchiseContract
            {
                Code = franchiseCode.Trim()
            };

            await Api.JoinFranchise(contract);
            
            await OnFranchiseJoined.InvokeAsync(franchiseCode);
            ResetForm();
            await OnClose.InvokeAsync();
        }
        catch (ApiException apiEx) when (apiEx.StatusCode == System.Net.HttpStatusCode.NotFound)
        {
            errorMessage = "Franchise not found. Please check the name and try again.";
        }
        catch (HttpRequestException)
        {
            errorMessage = "Unable to join franchise. Please check your connection and try again.";
        }
        catch (Exception ex)
        {
            errorMessage = "An unexpected error occurred. Please try again.";
            await Console.Error.WriteLineAsync($"Error joining franchise: {ex.Message}");
        }
        finally
        {
            isSubmitting = false;
        }
    }

    private void Close()
    {
        ResetForm();
        OnClose.InvokeAsync();
    }

    private void OnBackdropClick()
    {
        if (!isSubmitting)
        {
            Close();
        }
    }

    private void ResetForm()
    {
        franchiseCode = string.Empty;
        errorMessage = null;
        isSubmitting = false;
    }
}
